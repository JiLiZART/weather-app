import styled from '@emotion/styled';
import { sizeXl, sizeXs } from '../variables';

const Layout = styled.div`
	max-width: 992px;
	width: 100%;
	margin: 0 auto;
	padding: ${sizeXl} ${sizeXs};
	box-sizing: border-box;
`;

export default Layout;
